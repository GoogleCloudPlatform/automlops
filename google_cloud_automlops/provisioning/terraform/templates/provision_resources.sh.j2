#!/bin/bash
{{generated_license}}
# Submit terraform run creating all resources.
terraform -chdir={{base_dir}}provision init
terraform -chdir={{base_dir}}provision validate
terraform -chdir={{base_dir}}provision apply -auto-approve